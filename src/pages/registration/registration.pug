include ../../pug/_image.pug
include ../../pug/_head.pug
include ../../libs/_libs.pug

doctype html
html(lang="ru")
    +head('Registration', 'Registration')
    body
        header
            +header({
                existSignUp: true,
                isBold: false,
            })
        main
            .registration
                +card-account-signup('зарегистрироваться', './registration.html')
        footer
            +footer(true)
            +footer(false)
