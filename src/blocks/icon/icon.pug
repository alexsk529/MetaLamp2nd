mixin icon (str)
    - str = str || '';
    - let [icon, title, description] = str.split('/')
        .icon
            span.material-icons= icon
            .icon__text
                span.body= title
                span.body= description