mixin bullet-list(props)
    - props = props || {}
    - let {title = '', str = []} = props
        .bullet-list
            if (title)
                h3.label.label_position_button= title
            each val in str
                span.body= val
